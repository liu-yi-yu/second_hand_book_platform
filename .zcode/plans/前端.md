# 《二手书交易平台》前端实施计划（已按你的要求更新）

## 新增要求落实
1. **计划文档落盘**：新建 `docs/前端设计方案.md`，完整记录方案（技术选型、路由清单、文件结构、以 yu-test1 代码为准的接口适配清单、枚举对照表、已知后端问题、使用方式）。
2. **小白级详细注释**：所有 HTML/CSS/JS 文件均带详细中文注释——每个文件开头有"这个文件是干什么的/和后端哪个接口对应"的说明块，每个函数、每个关键逻辑、每个后端特殊行为（如取消订单要发 JSON 字符串、编辑书籍必须回传 image_ids 等）都有逐行级解释。

## 方案要点（同上次）
- 形式：纯 HTML/CSS/JS 哈希路由 SPA，放入 `src/main/resources/static/`，零依赖零构建，启动后端后访问 http://localhost:8081/（同源无 CORS）。
- 范围：全部功能——注册登录、发布/浏览/搜索书籍、购物车结算、订单状态流转、WebSocket 实时聊天、互评、通知、管理后台（看板/用户/书籍/订单仲裁）。
- 后端代码一行不改；`PUT /api/users/me` 缺 WHERE 的问题在编辑资料界面放置醒目风险提示。
- 接口契约以 yu-test1 代码为准（端口 8081、code=1 成功、登录用用户名、分页 {records,total} 与 pageNum/pageSize、排序传 SQL 片段、订单 ID 为整数、取消订单 body 为 JSON 字符串、评价仅 received 状态、通知默认只返回未读等，全部在计划文档中列表化）。

## 文件清单（全部新建，约 21 个文件）
```
docs/前端设计方案.md            ← 计划文档
src/main/resources/static/
├── index.html  ├── css/app.css
└── js/ app.js（入口） constants.js store.js api.js ui.js ws.js router.js
    └── views/ auth.js home.js bookDetail.js publish.js cart.js orders.js
              orderChat.js profile.js userProfile.js notifications.js admin.js
```

## 验证
全部文件生成后做 JS 语法校验（node --check 逐个文件）+ 按接口清单逐项核对路径/字段；最后交付使用说明与后端已知问题清单。