<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.tlais.yutest1.mapper.AdminMapper">
    <update id="updateStatus">
        update users
        set status = #{status}
        where id = #{id}
    </update>

    <select id="getPage" resultType="org.tlais.yutest1.domain.vo.UserProfileVO">
        select * from users
        <where>
            <if test="admin.keyword != null">
                username like concat('%',#{admin.keyword},'%')
                or email like concat('%',#{admin.keyword},'%')
            </if>
            <if test="admin.role != null">
                role = #{admin.role}
            </if>
            <if test="admin.status != null">
                status = #{admin.status}
            </if>
        </where>
    </select>
</mapper>
